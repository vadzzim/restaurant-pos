{
  "name": "restaurant-pos-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build:packages": "pnpm --filter @pos/config --filter @pos/contracts --filter @pos/domain --filter @pos/db run build",
    "dev": "pnpm run build:packages && pnpm --filter @pos/api --filter @pos/web --filter @pos/worker --parallel run dev",
    "lint": "eslint . && prettier --check --ignore-unknown .",
    "typecheck": "pnpm run build:packages && pnpm --filter @pos/api --filter @pos/web --filter @pos/worker run typecheck && tsc --noEmit -p tsconfig.e2e.json",
    "test": "pnpm run build:packages && pnpm --recursive --workspace-concurrency=1 --if-present run test",
    "test:unit": "pnpm run build:packages && pnpm --filter @pos/domain --filter @pos/web run test",
    "test:e2e": "node scripts/verify-e2e.mjs",
    "test:e2e:run": "playwright test",
    "verify:integration": "node scripts/verify-integration.mjs",
    "verify:multi": "node scripts/verify-multi-instance.mjs",
    "build": "pnpm run build:packages && pnpm --filter @pos/api --filter @pos/web --filter @pos/worker run build",
    "db:migrate": "pnpm run build:packages && pnpm --filter @pos/db run db:migrate",
    "db:seed": "pnpm run build:packages && pnpm --filter @pos/db run db:seed",
    "format": "prettier --write --ignore-unknown .",
    "format:check": "prettier --check --ignore-unknown .",
    "db:generate": "pnpm run build:packages && pnpm --filter @pos/db run db:generate",
    "db:check": "pnpm run build:packages && pnpm --filter @pos/db run db:check"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^24.13.3",
    "@vitejs/plugin-vue": "^6.0.8",
    "eslint": "^10.9.1",
    "eslint-plugin-vue": "^10.10.0",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "tailwindcss": "^4.3.3",
    "tsup": "^8.5.1",
    "tsx": "^4.23.12",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.68.0",
    "vite": "^8.2.2",
    "vue-tsc": "^3.3.11"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
